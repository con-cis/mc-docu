/**
 * Interface representing metadata information
 * @interface MetaData
 * @property {string} date - The date value
 * @property {string} version - The version number/string
 */
export interface MetaData {
  date: string
  version: string
}
