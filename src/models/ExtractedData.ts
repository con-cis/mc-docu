import { ChannelData } from './'

/**
 * Represents data extracted from channels
 * @interface ExtractedData
 */
export interface ExtractedData {
  /**
   * Array of channel data objects
   */
  channels: ChannelData[]
}
